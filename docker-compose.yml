version: '3.9'

services:

  api:
    build: .
    container_name: metadata_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7
    container_name: metadata_mongodb
    restart: always
    ports:
      - "27017:27017"